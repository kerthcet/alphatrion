apiVersion: v1
kind: Service
metadata:
  name: {{ include "alphatrion.server.fullname" . }}
  labels:
    {{- include "alphatrion.server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.server.service.type }}
  ports:
    - port: {{ .Values.server.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alphatrion.server.selectorLabels" . | nindent 4 }}
